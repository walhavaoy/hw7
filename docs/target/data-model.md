# Data Model

## Overview

This application has no persistent data layer. There are no databases, no entities, and no data relationships.

## Entities

None. The application serves only static content.

## ER Diagram

```mermaid
erDiagram
    %% No entities — static content only
```

## Notes

- No database or persistent storage is required
- All content is served from static files at build time
